meta {
  name: Créer un todo
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/todo
  body: json
  auth: inherit
}

body:json {
  {
  "text":"Salut"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
