meta {
  name: Recup user
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/user
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "email": "test@test.com",
    "password": "123456"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
